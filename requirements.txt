# Core LLM
anthropic>=0.49.0

# Config / validation
pydantic>=2.6
python-dotenv>=1.0

# RAG: vector store + API
psycopg[binary]>=3.1
pgvector>=0.2.5
fastapi>=0.110
uvicorn>=0.29

# Embeddings (Voyage default; local fallback)
voyageai>=0.2.3
sentence-transformers>=2.6     # optional local-embedding fallback

# Sparse retrieval helper (client-side BM25 fallback if not using Postgres FTS)
rank-bm25>=0.2.2
